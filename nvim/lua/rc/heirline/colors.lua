local palette = require("nightfly").palette

return {
  bg = palette.black_blue,
  fg = palette.white,
  red = palette.red,
  green = palette.green,
  blue = palette.blue,
  orange = palette.orange,
  magenta = palette.purple,
  cyan = palette.cyan_blue,
  pink = palette.peach,
  diag_warn = palette.yellow,
  diag_error = palette.red,
  diag_hint = palette.blue,
  diag_info = palette.white,
  git_add = palette.green,
  git_change = palette.blue,
  git_del = palette.red,
}
